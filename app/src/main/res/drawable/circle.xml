<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval" >
    <solid android:color="@android:color/transparent" />

    <size
        android:height="16dp"
        android:width="16dp" />

    <!--This is the stroke for the button-->
    <!--<stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" />-->
    <solid android:color="@color/colorAccentBackgroundButton"/>
</shape>